const router =require('express').router();
const fs =require('fs');
const util = require ('util');




router.get('/notes',)
{
    then(filecontents) 
        resizeBy.json(json.parse(filecontents));
};

router.post('note',) 
{
    const note = req.body;
    then(filecontents)
    const allnotes = json.parse(filecontents);
    allnotes.push(notes);
    
};

module.exports = router;
